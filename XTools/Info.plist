<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>XTools</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
    <key>LSUIElement</key>
    <false/>
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>
    <!-- Sparkle auto-update (PLACEHOLDER — replace once the GitHub repo + EdDSA
         signing key exist; until then "Check for Updates" will fail to find a feed). -->
    <key>SUFeedURL</key>
    <string>https://github.com/XueshiQiao/XTools/releases/latest/download/appcast.xml</string>
    <!-- XTools' dedicated Sparkle EdDSA public key (keychain account "xtools";
         private key is the GitHub secret SPARKLE_EDDSA_KEY). -->
    <key>SUPublicEDKey</key>
    <string>KL9GQksqlNEUTISeFlPRNvKzHXmKgOJ1B5wq9ZlcpVo=</string>
    <!-- Background update checks. Until the first release publishes appcast.xml,
         scheduled checks fail silently; once a release exists they pick it up. -->
    <key>SUEnableAutomaticChecks</key>
    <true/>
</dict>
</plist>
